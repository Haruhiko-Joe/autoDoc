# autoDoc 竞品横向对比分析

## 一、竞品全景图

### Tier 1: AI 仓库级 Wiki 生成器（直接竞品）

| 工具 | 背景 | 核心方式 | 开源 | 定价 |
|------|------|----------|------|------|
| **DeepWiki** (Cognition/Devin) | 市场领导者，50K+ 仓库已索引，4B+ LOC | LLM 分析 + RAG 问答 + Mermaid 图 + MCP | 否 | 公开仓库免费，私有 $20 起 |
| **Google Code Wiki** | Google 2025 新品，Gemini 驱动 | 每次代码变更后自动重新生成 | 否 | 公开预览免费 |
| **CodeWiki (FSoft)** | 学术框架 (ACL 2026) | 三阶段：层级分解 → 多 Agent → 多模态合成 | 是 | 免费 |
| **DeepWiki-Open** (AsyncFuncAI) | DeepWiki 开源仿制品 | 多模型支持 (Gemini/OpenAI/Ollama) | 是 | Token 计费 $0-49/月 |
| **OpenDeepWiki** (AIDotNet) | C#/TypeScript 开源方案 | 模块化知识管理 | 是 | 免费 |
| **Zread.ai** | 2025 新品 | URL 替换，生成业务需求 + 技术文档 + 测试策略 | 否 | 免费 |
| **GitSummarize** | 2025 新品 | URL 替换，即时文档中心 | 否 | 免费 |
| **Code2Tutorial** | 开源 | 面向新人教程风格，非架构文档 | 是 | 免费 |
| **deepwiki-rs (Litho)** | Rust 实现 | 高性能 C4 架构文档生成 | 是 | 免费 |

### Tier 2: AI 文档平台

| 工具 | 核心特点 | 定价 |
|------|----------|------|
| **Mintlify** | docs-as-code + AI 写作助手 + MCP Server | $250/月 |
| **Swimm** | 代码变更自动同步文档，解决文档过时问题 | 订阅制 |
| **ReadMe** | 交互式 API Explorer + 实时测试 | $349/月 |
| **DocuWriter.ai** | NLP 驱动 + UML 图 + 多语言 | 订阅制 |
| **Sourcegraph Cody** | AI 编码助手 + 语义搜索 + 开源 | 免费/企业版 |
| **Archbee** | 轻量编辑器 + AI Write Assist | $50-230/月 |

### Tier 3: 静态文档生成器（基线）

| 工具 | 语言 | 方式 |
|------|------|------|
| **Doxygen** | C/C++/Python/Java 等 | 注释提取，HTML/PDF/LaTeX |
| **TypeDoc** | TypeScript | 编译器 API 精确类型保留 |
| **JSDoc** | JavaScript | 注释标注，行业标准 |
| **Sphinx** | Python 为主 | reStructuredText，多格式输出 |
| **Rustdoc** | Rust | 语言内置，示例自动编译测试 |
| **GoDoc** | Go | 标准注释提取，极简 |
| **Javadoc** | Java | 行业标准，企业广泛使用 |

### Tier 4: Wiki / 知识平台

| 工具 | 定位 | 特点 |
|------|------|------|
| **GitBook** | Git 原生文档平台 | 版本控制 + PR 式审核 |
| **Confluence** | 企业 Wiki | 20 年历史，60K+ 客户，实时协作 |
| **Notion** | 全能工作空间 | 灵活数据库 + 看板 + Wiki |

### Tier 5: 图表 / 可视化工具

| 工具 | 方式 |
|------|------|
| **Mermaid** | Markdown 风格语法，GitHub 原生支持 |
| **PlantUML** | DSL 语言 + Graphviz |
| **Structurizr** | C4 模型专用，支持导出 Mermaid/PlantUML |

---

## 二、评价维度体系（12 维度，1-5 分）

### D1. 文档生成方式

| 分 | 标准 |
|----|------|
| 1 | 纯手动编写 |
| 2 | 注释提取（Doxygen 式）或基础 AI 建议 |
| 3 | AI 单次生成，无验证循环 |
| 4 | 多步骤 AI 管线，有部分验证 |
| 5 | 多 Agent 管线 + 迭代验证 + 结构化输出 Schema + 重试机制 |

### D2. 内容质量与深度

| 分 | 标准 |
|----|------|
| 1 | 浅层摘要，频繁错误 |
| 2 | 基本类/文件描述，偶有不准确 |
| 3 | 准确的模块描述 + 主要 API 覆盖 |
| 4 | 深度架构分析 + 函数签名 + 类型定义 + 配置项 |
| 5 | 专家级：流程走查 + 数据流分析 + 边界情况 + 带行号的代码片段 |

### D3. 结构组织

| 分 | 标准 |
|----|------|
| 1 | 扁平结构，无层级 |
| 2 | 两级层级 + 基本目录 |
| 3 | 多级层级 + 侧边栏导航 + 基本搜索 |
| 4 | 深层级 + 渐进式展开 + 全文搜索 + 面包屑 |
| 5 | 动态深度递归图结构 + Agent 自主决定分解粒度 + 图导航 + 树导航 + 搜索 |

### D4. 可视化与交互

| 分 | 标准 |
|----|------|
| 1 | 纯文本输出，无图表 |
| 2 | 静态图表（Mermaid 图片渲染） |
| 3 | 可交互图表（缩放/平移）或无上下文的 Chat |
| 4 | 交互式图可视化 + 带项目上下文的 Chat |
| 5 | 6+ 类型化边的交互图 + 边悬浮详情 + 交互式序列图 + 上下文 Chat + 实时生成进度 |

### D5. 开发者体验

| 分 | 标准 |
|----|------|
| 1 | 复杂多步安装 + 陡峭学习曲线 + 重维护 |
| 2 | 中等安装（5-10 步）+ 需要配置 |
| 3 | 简单安装（3-5 步）+ 直观 UI + 低维护 |
| 4 | 一条命令或 URL 式使用 + 零配置 |
| 5 | 零摩擦（粘贴 URL 或一键）+ 即时结果 + 开箱即用 |

### D6. 代码新鲜度与同步

| 分 | 标准 |
|----|------|
| 1 | 无同步机制，文档是一次性快照 |
| 2 | 手动重跑 + 增量处理可跳过已完成节点 |
| 3 | CLI/API 可触发重新生成 + 可手动集成 CI/CD |
| 4 | 内置 CI/CD 集成 + 变更文件自动检测 + 选择性重新生成 |
| 5 | 每次提交/PR 自动同步 + 实时更新 + 文档版本控制 |

### D7. 可扩展性（规模）

| 分 | 标准 |
|----|------|
| 1 | 仅处理小项目 (<10K LOC)，单语言，顺序处理 |
| 2 | 中等项目 (10K-100K LOC)，少数语言，有限并发 |
| 3 | 大型项目 (100K-500K LOC)，5+ 语言，中等并发 |
| 4 | 超大型 (500K-1M LOC)，语言无关，可配置并发 |
| 5 | 百万级 LOC 经验证，完全语言无关，分布式执行 |

### D8. 定制与扩展性

| 分 | 标准 |
|----|------|
| 1 | 无定制能力，固定输出格式和外观 |
| 2 | 基本主题（明/暗）+ 固定输出格式 |
| 3 | 可配置模板或输出格式，部分插件支持 |
| 4 | 多种输出格式 + 可切换 AI 后端 + 可配置行为 |
| 5 | 完整插件生态 + 自定义 Agent 指令 + 每角色可选模型 + 格式灵活 + API |

### D9. 部署与定价

| 分 | 标准 |
|----|------|
| 1 | 仅闭源 SaaS + 昂贵 (>$300/月) + 无自托管 |
| 2 | SaaS + 有限免费层 + 中等价格 + 无自托管 |
| 3 | 开源可自托管 + 需付费 AI API Key |
| 4 | 开源自托管 + 免费层或低成本 + 灵活部署 |
| 5 | 完全开源 + 自托管 + 免费 + 自带模型 + 最低基础设施要求 |

### D10. 协作能力

| 分 | 标准 |
|----|------|
| 1 | 单用户，无任何协作功能 |
| 2 | 可共享输出文件，无实时协作 |
| 3 | 多用户访问 + 基本评论功能 |
| 4 | 团队空间 + 审核流程 + 版本历史 |
| 5 | 实时协作编辑 + 审核流程 + RBAC + 审计日志 |

### D11. 集成生态

| 分 | 标准 |
|----|------|
| 1 | 独立工具，无任何集成 |
| 2 | 基本 HTTP API，可脚本化接入 |
| 3 | VCS 集成 (GitHub/GitLab) + API |
| 4 | IDE 集成 / VCS 集成 / CI/CD 集成 或 MCP 支持 |
| 5 | 完整生态：IDE + VCS + CI/CD + MCP + 第三方 API + Webhook |

### D12. 容灾与可靠性

| 分 | 标准 |
|----|------|
| 1 | 无恢复机制，失败需全部从头重跑 |
| 2 | 基本检查点，可部分恢复 |
| 3 | 状态持久化 + 手动恢复 + 错误重试 |
| 4 | 自动崩溃恢复 + 增量处理 + 节点级错误隔离 |
| 5 | Session ID 恢复 + 自动重试循环 + pending 暂存目录 + 零数据丢失 |

---

## 三、横向对比评分表

| 维度 | autoDoc | DeepWiki | Google Code Wiki | CodeWiki (FSoft) | DeepWiki-Open | Mintlify | Swimm | Sourcegraph Cody | Doxygen |
|------|:-------:|:--------:|:----------------:|:----------------:|:-------------:|:-------:|:-----:|:----------------:|:-------:|
| D1. 生成方式 | **5** | 4 | 3 | 4 | 3 | 3 | 3 | 2 | 2 |
| D2. 内容质量 | **4** | 4 | 3 | 4 | 3 | 3 | 3 | 3 | 2 |
| D3. 结构组织 | **5** | 4 | 3 | 4 | 3 | 4 | 3 | 2 | 3 |
| D4. 可视化交互 | **5** | 4 | 3 | 3 | 3 | 2 | 2 | 2 | 2 |
| D5. 开发者体验 | **5⚡** | **4** | **4** | 2 | 3 | 4 | 4 | 4 | 3 |
| D6. 代码新鲜度 | **5** | 2 | **5** | 1 | 2 | 3 | **5** | 3 | 2 |
| D7. 可扩展性 | 4 | **5** | **5** | 3 | 3 | 4 | 4 | **5** | 4 |
| D8. 定制扩展 | **4** | 2 | 2 | 3 | 4 | 4 | 3 | 3 | 3 |
| D9. 部署定价 | 4 | 4 | 4 | **5** | **5** | 2 | 2 | 4 | **5** |
| D10. 协作能力 | **4** | 2 | 3 | 1 | 1 | **4** | **4** | 3 | 1 |
| D11. 集成生态 | **4** | 4 | 4 | 1 | 2 | **5** | 4 | **5** | 3 |
| D12. 容灾可靠性 | **5** | 3 | 4 | 2 | 2 | 3 | 3 | 3 | 3 |
| **总分 (/60)** | **54** | **43** | **44** | **33** | **34** | **38** | **37** | **36** | **30** |

> ⚡ doc-drill skill 使用体验远超竞品（详见下文说明）

---

## 四、autoDoc 优势分析

### 核心优势（行业领先）

#### 1. 多 Agent 迭代验证管线（D1=5，行业唯一）

autoDoc 采用 5 个 Agent（Scaffold / Decomposer / Writer / Checker / FlowAnalyzer）协作的管线架构，每个阶段均经过 Checker Agent 独立验证。

- **验证项**：broken-target、invalid-path、empty-content、missing-section、missing-ref
- **重试机制**：失败后带结构化修复提示的重试循环（最多 5 次），通过 `agent.continue()` 保持会话连续性
- **结构化输出**：所有 Agent 输出经 Zod Schema 强制校验

**无任何竞品达到同等级别的质量保障机制。** DeepWiki 和 Google Code Wiki 均为单次生成，无公开的迭代验证循环。

#### 2. 动态深度递归图结构（D3=5）

Decomposer Agent 根据代码复杂度自主决定分解深度，而非使用固定层级。产出的文档层级自然映射代码库自身的复杂度结构。

- 支持任意嵌套深度：`child: { type: "graph" | "page" }`
- 竞品（DeepWiki / Google Code Wiki）多为固定深度或扁平文件列表结构

#### 3. 最丰富的交互式可视化（D4=5，最大差异化特性）

| 特性 | autoDoc | DeepWiki | Google CW | CodeWiki |
|------|---------|----------|-----------|----------|
| 语义边类型数 | 6 种 | 无 | 无 | 无 |
| 交互式图导航 | AntV G6 | 无 | 无 | 无 |
| 边悬浮弹窗 | 有 | 无 | 无 | 无 |
| 交互式时序图 | FlowsPage | 无 | 有（静态） | 有（静态） |
| 实时生成进度 | SSE 动画进度条 | 无 | 无 | 无 |

6 种语义边类型：`calls` / `depends` / `data-flow` / `event` / `extends` / `composes`，每种边具有独立视觉样式（颜色、线型、箭头类型）。

#### 4. 企业级容灾恢复（D12=5）

- 每个图 JSON 文件嵌入 Session ID（decomposerSessionId, checkerSessionId, writerSessionIds）
- 崩溃重启后 `resetRecoverableNodes()` 自动扫描并恢复中间状态节点
- Writer 输出暂存于 `_pending/` 目录，已完成的 Writer 不因中断而丢失
- 节点级错误隔离：单个节点失败不影响其他节点

#### 5. 混合 Agent 后端（D8=4）

5 个角色各自独立选择 Claude 或 Codex 后端，可从前端 UI 直接配置，支持成本/速度优化组合。

#### 6. 祖先上下文传播

每次 Decomposer / Writer 调用携带完整层级上下文（AncestorContext），包含父层级模块名、描述、兄弟节点、边关系，避免深层递归产生重复或矛盾内容。

---

## 五、autoDoc 劣势分析

### 待改进项

#### 1. 代码新鲜度与同步（D6=5，已实现）

PR 驱动的增量更新管线已上线：

- **PR/commit 粒度**：通过 `gh pr list`（GitHub 项目）或 `git log --first-parent`（非 GitHub）自动发现所有新合并的 PR/commit，按时间升序逐条处理
- **Agent 自主导航**：PrUpdater Agent 接收 commit metadata + diff，通过 MCP 工具（`get_top` → `search_nodes` → `get_page` → `patch_page`）自主定位并做针对性修改，不再依赖 codeScope 静态匹配
- **两阶段 prompt**：Agent 先做影响评估（none / minor / structural），无影响的 PR 直接短路跳过；有影响的才执行 MCP 写操作
- **两种模式**：Auto（流水线式连续处理）、Manual（每条 PR 都经过用户审阅闸门 + 可追加提示词让 agent 续写微调）
- **文档版本控制**：每次写入带 baseVersion 乐观锁 + `.history/` 快照，支持 `revert` 回滚
- **cursor 持久化**：`lastProcessedSha` 记录在 `project-meta.json`，服务重启后从断点继续

| 工具 | 代码同步能力 |
|------|-------------|
| Google Code Wiki | 每次代码变更后自动重新生成（D6=5） |
| Swimm | 代码变更自动检测并更新文档（D6=5） |
| **autoDoc** | **按 PR 粒度增量更新 + 影响评估短路 + 版本控制 + 手动审阅闸门（D6=5）** |
| Mintlify | Git 集成 + CI/CD 触发（D6=3） |

#### 2. 协作能力（D10=4，已实现）

- **中心化部署**：后端服务统一部署，团队所有成员通过 Web UI 共同使用
- **手动审阅闸门**：Manual 模式下每条 PR 的文档更新都经过 `awaiting-review` 闸门，用户可 Accept 或追加提示词要求 agent 微调（session 续写）
- **版本历史**：所有文档变更自动保存到 `.history/`，可随时 `revert` 回滚
- **MCP 可写**：团队中的 Code Agent 可通过 MCP 工具直接读写文档，乐观锁防止并发冲突

#### 3. 集成生态（D11=4）

| 集成类型 | autoDoc | DeepWiki | Mintlify |
|----------|---------|----------|---------|
| Code Agent 集成 | **doc-drill Skill（自动安装）** | 无 | 无 |
| MCP Server | **有（HTTP Streamable，query + mutate + 版本控制）** | 有 | 有 |
| CI/CD 集成 | 无 | 无 | 有 |
| VCS 集成 | **有（git fetch + gh pr list 自动发现 PR）** | GitHub | GitHub |
| HTTP API | 有（run/status/doc/search/chat/update） | 有 | 完整 REST |
| SSE 实时流 | **有（生成进度 + 增量更新队列）** | 无 | 无 |

> autoDoc 的 doc-drill skill + HTTP MCP 的组合是独一无二的 Agent-native 集成：任何 code agent 都能通过它自顶向下导航文档，理解函数的作用、影响范围和全局位置，并且直接通过 MCP 工具维护文档。这是 DeepWiki（仅网页 Chat）和 Mintlify（仅文档站点）所不具备的能力。

#### 4. 开发者体验（D5=5）

**安装体验：** 相比 DeepWiki（URL 替换）和 Google Code Wiki（URL 替换），autoDoc 需要 Node.js + pnpm + Claude Code 安装，首次安装有一定摩擦。

**生成后使用体验（领先竞品）：** autoDoc 自动生成 `doc-drill` Claude Code Skill 并安装到目标仓库的 `.claude/skills/` 目录，提供：
- **渐进式浏览**（lazy-load，节省上下文）：从顶层模块逐步深入到实现细节
- **关系追踪**：沿 6 种语义边追踪模块间调用链和数据流
- **关键词搜索**：跨所有文档层级搜索
- **代码范围映射**：每个节点标注覆盖的源文件/目录
- **业务流程导航**：通过 `flows.json` 理解端到端交互场景

任何 code agent 都能通过 doc-drill **自顶向下理解一个函数的作用、影响范围和在全局架构中的位置**。这是 DeepWiki（仅网页 Chat）和 Google Code Wiki（仅网页浏览）不具备的深度 Agent 集成能力。

#### 5. Chat 功能待增强

当前 Chat 使用 OpenAI 通用对话，无 RAG 或文档上下文注入。加入 RAG 模式后可显著提升 Chat 的针对性和准确度。

#### 6. 无测试套件

`pnpm test` 是 stub，对开源项目的可信度和社区采纳有一定影响。

---

## 六、雷达图数据

维度顺序：生成方式 / 内容质量 / 结构组织 / 可视化交互 / 开发者体验 / 代码新鲜度 / 可扩展性 / 定制扩展 / 部署定价 / 协作 / 集成 / 容灾

```json
[
  { "tool": "autoDoc",          "scores": [5, 4, 5, 5, 5, 5, 4, 4, 4, 4, 4, 5] },
  { "tool": "DeepWiki",         "scores": [4, 4, 4, 4, 4, 2, 5, 2, 4, 2, 4, 3] },
  { "tool": "Google Code Wiki", "scores": [3, 3, 3, 3, 4, 5, 5, 2, 4, 3, 4, 4] },
  { "tool": "CodeWiki (FSoft)", "scores": [4, 4, 4, 3, 2, 1, 3, 3, 5, 1, 1, 2] },
  { "tool": "DeepWiki-Open",    "scores": [3, 3, 3, 3, 3, 2, 3, 4, 5, 1, 2, 2] },
  { "tool": "Mintlify",         "scores": [3, 3, 4, 2, 4, 3, 4, 4, 2, 4, 5, 3] },
  { "tool": "Swimm",            "scores": [3, 3, 3, 2, 4, 5, 4, 3, 2, 4, 4, 3] },
  { "tool": "Sourcegraph Cody", "scores": [2, 3, 2, 2, 4, 3, 5, 3, 4, 3, 5, 3] },
  { "tool": "Doxygen",          "scores": [2, 2, 3, 2, 3, 2, 4, 3, 5, 1, 3, 3] }
]
```

---

## 七、战略改进建议

### P0 - 已完成 ✅

| # | 建议 | 状态 | 影响 | 对标竞品 |
|---|------|------|------|----------|
| 1 | **PR 驱动增量更新** — 按 PR 粒度、Agent 自主导航、影响评估短路、版本控制 | ✅ 已上线 | 高 | Swimm, Google CW |
| 2 | **审阅闸门 + 多用户协作** — Manual 模式审阅确认、session 续写微调、中心化部署 | ✅ 已上线 | 高 | Mintlify, Swimm |
| 3 | **HTTP MCP Server** — query + mutate + 版本控制 + 乐观锁 | ✅ 已上线 | 高 | DeepWiki, Mintlify |
| 4 | **精准编辑工具 patch_page** — string match + replace 精细修改，替代全量 update_page | ✅ 已上线 | 中 | — |

### P1 - 建议优先建设

| # | 建议 | 工作量 | 影响 | 对标竞品 |
|---|------|--------|------|----------|
| 5 | **增强 Chat 为 RAG 模式** — 将当前页面 Markdown + 图上下文注入 chat 消息 | 低 | 高 | DeepWiki |
| 6 | **Docker 一键部署** — `docker-compose up` 零前置依赖部署 | 低 | 高 | 降低安装门槛 |

### P2 - 差异化机会

| # | 建议 | 工作量 | 影响 |
|---|------|--------|------|
| 6 | **文档版本控制** — 记录 "基于 commit X 生成"，支持文档演进追踪 | 中 | 中 |
| 7 | **静态站点导出** — 生成无需后端服务器的独立 HTML 站点 | 中 | 中 |
| 8 | **添加测试套件** — Arranger 状态机、Schema 验证、图 I/O 的单元测试 | 中 | 中 |

### P3 - 长期愿景

| # | 建议 | 工作量 | 影响 |
|---|------|--------|------|
| 9 | **VS Code 扩展** — 在 IDE 内浏览文档，支持代码 ↔ 文档双向导航 | 高 | 中 |
| 10 | **分布式执行** — 分布式 Worker 池处理超大仓库 | 高 | 低 |

---

## 八、总结与定位

### 竞争格局定位

| 对比 | autoDoc 定位 |
|------|-------------|
| vs DeepWiki | autoDoc 是 **"深度派"**（验证 + 可视化），DeepWiki 是 **"便捷派"**（URL 即用 + MCP） |
| vs Google Code Wiki | Google Code Wiki 内容单薄、结构扁平、可视化为静态图表，仅在代码新鲜度和规模上有平台优势。autoDoc 在 **内容深度、结构组织、交互可视化、生成质量保障** 上全面领先 |
| vs CodeWiki (FSoft) | 同为多 Agent 开源方案，autoDoc 在 **可视化和容灾** 上领先，CodeWiki 在 **学术评测分数** 上领先 |
| vs Swimm | Swimm 在 **文档同步** 上独步天下，autoDoc 在 **架构理解深度** 上更强 |
| vs Mintlify | Mintlify 在 **集成生态** 上完胜，autoDoc 在 **生成质量保障** 上更强 |

### autoDoc 的独特护城河

1. **唯一** 具有多 Agent 迭代验证循环的开源代码文档方案
2. **唯一** 具有 6 种语义边类型交互图的文档工具
3. **唯一** 具有企业级 Session 容灾恢复的开源文档生成器
4. **唯一** 支持每角色独立切换 AI 后端的方案
5. **唯一** 自动生成 Code Agent Skill（doc-drill），让 AI 编程助手原生理解代码架构
6. **唯一** 具有 PR 粒度增量更新 + 人工审阅闸门 + Agent session 续写微调的文档维护系统

### autoDoc 的竞争展望

当前总分 **54/60**，大幅领先 Google Code Wiki (44) 和 DeepWiki (43)。PR 驱动增量更新、HTTP MCP Server、手动审阅闸门 + session 续写已全部上线，此前标记为 🚧 的 D6/D10/D11 短板均已补齐。

当前领先优势集中在：生成质量保障（D1=5）、可视化交互（D4=5）、代码同步（D6=5）、容灾恢复（D12=5）四个维度同时满分，无任何竞品达到同等综合水平。

下一步竞争策略：Docker 一键部署降低安装门槛（D5→5+）、RAG 增强 Chat 精准度（D2→5）、CI/CD webhook 自动触发（D6 进一步强化至全自动）。